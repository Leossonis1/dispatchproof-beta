# DispatchProof V2.40.5 Patch Only

Fixes **Readiness Request → Copy Link**.

Copy Link now reads the exact browser-resolved URL from the working **Open External Page** button, so both actions always use the same readiness link.

## Install
Replace the matching files in your current DispatchProof project with the files in this patch, commit/push to GitHub, and let Render redeploy.

No database migration or reset is required.

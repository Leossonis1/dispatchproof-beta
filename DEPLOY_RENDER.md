# DispatchProof V2.46.1 Render Deploy

Deploy over V2.46.0. No database migration and no environment-variable changes are required.

Keep the existing persistent storage setting unchanged:

`DISPATCHPROOF_DATA_DIR=/var/data/dispatchproof`

## First QA
Upload one supported document to a saved subcontractor and confirm it appears in the subcontractor document list without a 500 error.
